{
    "extends": "astro/tsconfigs/strict",

    /* - Sirve para haces un espacio de nombres similar al que usamos en PHP */
    "compilerOptions": {
        "baseUrl": ".",
        "paths": {
            "cv": ["./cv.json"],
            "@/*": ["src/*"]
        }
    }
}
